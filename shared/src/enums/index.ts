export enum MatchStatus {
  PENDING = "PENDING",
  MATCHED = "MATCHED",
  REJECTED = "REJECTED",
}

export enum SwipeDirection {
  LEFT = "LEFT",
  RIGHT = "RIGHT",
}

export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum PropertyType {
  APARTMENT = "APARTMENT",
  HOUSE = "HOUSE",
  STUDIO = "STUDIO",
  LOFT = "LOFT",
}
